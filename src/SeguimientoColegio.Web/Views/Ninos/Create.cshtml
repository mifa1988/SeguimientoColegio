@model NinoFormPageViewModel
@{
    ViewData["Title"] = "Nuevo nino";
    ViewData["Subtitle"] = "Crea un nuevo perfil de seguimiento escolar.";
}

<section class="page-card">
    <form method="post">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly"></div>
        <partial name="_Form" model="Model.Form" />
        <div class="form-actions mt-3">
            <button type="submit" class="btn-app btn-app--primary">Guardar</button>
            <a asp-action="Index" class="btn-app btn-app--ghost">Volver</a>
        </div>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
