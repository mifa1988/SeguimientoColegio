@model CorreoOrigenFormPageViewModel
@{
    ViewData["Title"] = "Nuevo correo origen";
    ViewData["Subtitle"] = "Registra un correo relevante que luego podra vincularse a uno o varios registros.";
}

<section class="page-card">
    <form method="post">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly"></div>
        <partial name="_Form" model="Model.Form" />
        <div class="form-actions mt-3">
            <button type="submit" class="btn-app btn-app--primary">Guardar</button>
            <a asp-action="Index" class="btn-app btn-app--ghost">Volver</a>
        </div>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
